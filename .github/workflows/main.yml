name: CI

on: 
  pull_request:
    branches:
    - master
    

jobs:
  analyze:
    runs-on: ubuntu-latest
    container:
      image: cirrusci/flutter:stable

    steps:
    - uses: actions/checkout@v1
    - run: touch /github/home/.flutter
    - name: Flutter analyze
      run: flutter analyze
